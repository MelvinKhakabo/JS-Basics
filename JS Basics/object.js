// Declaring objects in JS
let person = {
    name: 'John',
    age: 30
};
console.log(person);

//Changing values in objects
//dot notation
person.name = 'Mary';
console.log(person.name);
//bracket notation
person['name'] = 'Mike';
console.log(person.name);